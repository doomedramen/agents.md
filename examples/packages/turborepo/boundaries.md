# Workspace boundaries

- Keep package ownership and dependency direction explicit at workspace boundaries.
- Put shared configuration in the smallest common package that needs it.
- Validate affected workspace packages with the repository's configured task pipeline.
