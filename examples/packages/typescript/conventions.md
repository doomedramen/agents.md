# TypeScript conventions

- Keep compiler settings strict and make public module boundaries explicit.
- Prefer narrow types and explicit error handling over unchecked assertions.
- Validate with the repository's configured typecheck and test commands.
