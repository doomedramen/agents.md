# TypeScript example package

Audience: teams maintaining TypeScript repositories.

Assumptions: project uses TypeScript and already defines its own build and test
commands. Exclusions: this package does not prescribe a framework, runtime,
directory layout, or debugging workflow.

License: MIT. Example content is provided for local Git walkthroughs.
